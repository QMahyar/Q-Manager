/**
 * Motion components barrel export.
 *
 * These are intentionally lightweight: page fades and hover/press feedback are
 * pure CSS; only list add/remove uses a (cheap) JS animation via auto-animate.
 */
export { PageTransition } from "./PageTransition";
export { AnimatedCard, AnimatedCardContainer } from "./AnimatedCard";
export { AnimatedList } from "./AnimatedList";
export { AnimatedBadge } from "./AnimatedBadge";
export { AnimatedButton } from "./AnimatedButton";
